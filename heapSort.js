
/**
 * 
 * @param {Array} arr 
 */
function heapSort(arr) {
    var len = arr.length;
    for (var i = Math.floor(len / 2)- 1; i >=0; i--) {
        max_heapify(i, len);
        for (var i = len - 1; i > 0; i--) {
            
        }
    }
}

function swap() {

}

